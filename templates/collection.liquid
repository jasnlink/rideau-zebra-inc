<div class="pt-4 pb-12 lg:pt-4 lg:pb-24 px-4 sm:px-8">
    <div class="grid grid-cols-1 sm:grid-cols-3 gap-x-4 gap-y-8">
        {% for product in collection.products %}
            {% render 'product-card', product: product %}
        {% endfor %}
    </div>
</div>